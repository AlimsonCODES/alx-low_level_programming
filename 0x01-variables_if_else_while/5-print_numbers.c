#include <stdio.h>

/**
 * main - print all single digit base 10 numbers
 *
 * Return: 0 (success)
 */

int main(void)
{
	char ch;

	for (ch = 0; ch <= 9; ch++)
	       putchar(ch);

	putchar(ch);
	return (0);
}
