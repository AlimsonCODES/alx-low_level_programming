#include "main.h"

/**
 * _isalpha - function that checks for alphabetic character
 *
 * Return: Always 0 (success)
 */

int _isalpha(int c)
{
	if (isalpha(c) != 0)
	{
		_putchar(c);
	}
	else
	{
		_putchar(c);
	}
}
