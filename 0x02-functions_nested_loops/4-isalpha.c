#include "main.h"
#include <stdio.h>

/**
 * _isalpha.c - function that checks for alphabetic character
 *
 * Return: 0 || 1 (success)
 */

int _isalpha(int c)
{
	char d;
	char e;

	if (d = 'a'; d <= 'z'; d++ || e = 'A'; e <= 'Z'; e++)
		return (1);
	else
		return (0);
}
